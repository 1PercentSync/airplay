# 决策记录

> **本文档是用户认可的三份文档之一**，另两份是 `REQUIREMENTS.zh-CN.md`（冻结需求）和 `AGENTS.md`（工作区规则）。其余所有文档都是 Agent 自己写的，未经用户逐条认可。
>
> **本文档以实际会话记录为准重建**；如与会话记录冲突，以会话记录为准。
> 会话记录文件：`/home/alex/.pi/agent/sessions/--mnt-d-Github-airplay--/2026-08-12T12-54-28-337Z_019ff609-d8f1-777b-8275-6bddabf3fd28.jsonl`。下文的"消息 N"指该文件里第 N 条消息。
>
> **记录原则**：用户点头（"采纳建议"/"对"/"行"）只代表认可**路线方向**。提议中附带的实现细节不是用户逐条审过的，单独围栏标注为"Agent 细节"，不属于批准内容。
>
> 旧版决策记录（`research/archive/02` §7）把提议细节混记为"用户确认"，作废，仅存档。

## 一、五项大决策

### 决策 A：技术栈

**用户原话**："rust没问题……airplay2-rs肯定不可能用,AI代码也不可信"（消息 123）；"采纳建议,下一个问题"（消息 125）。

**批准的路线**：用 Rust。协议代码自己写，不依赖现成 AirPlay 库。AI 生成的代码不可用。

> Agent 细节（提议时附带，未逐条认可）：白名单具体条目（RustCrypto / mdns-sd / windows / rubato / tokio）；SRP、TLV8、bplist、ALAC 帧四个组件自实现；NTP 先行、PTP 推迟；“以 airplay2-sender-cpp 为第一参照”（该项目后降级，实际工作已改用 owntone/pyatv 为仲裁）。

### 决策 B：流传输方式

**用户原话**："采纳建议,下一个"（消息 138）。

**批准的路线**：只用 realtime（实时流），不研究 buffered（缓冲流）。

> Agent 细节：复议条款（"真机实测 realtime 稳定性不达标时再复议"）。

### 决策 C：音质规格

**用户原话**："做研究,然后如果没有资料就44.1kHz/16bit/立体声"（消息 142）。

**批准的路线**：先调研；没有资料支持更高规格，就用 44.1kHz / 16bit / 立体声。——调研结论是无资料支持，按规则落定。

> Agent 细节：音质预算的具体投向（重采样质量、位深抖动、声卡重采样排查）。

### 决策 C 补充：预留高规格

**用户原话**："1.没问题"（消息 220）。

**批准的路线**：44.1kHz/16bit/立体声保底，同时给更高规格留协商的余地。

> Agent 细节：启用条件（按设备广告启用）、验证方式（真机）。
> 后续事实：当时支持该补充的第三方资料后被证伪；真机探测确认本设备只广告 44.1kHz/16bit/立体声（`research/06`），补充在本设备不会触发。

### 决策 D：虚拟声卡

**用户原话**："D1,已安装Steam Streaming Speakers,但不代表我们就用Steam Streaming Speakers,做调研"（消息 157）。

**批准的路线**：用第三方虚拟声卡，不自研驱动；用户机器上已装 Steam Streaming Speakers，但具体用哪款调研后再定（调研已完成，见 `research/04`）。

> Agent 细节：软件枚举设备 + 引导用户选择的具体交互。

### 决策 E：配对认证范围

**用户原话**："只需要支持无密码无认证的路线"（消息 161）。

**批准的路线**：只支持无密码、无认证这一条路线。

> Agent 细节：401/403/470 错误提示机制（旧记录曾误记为批准内容，已剔除）。

### 决策 E 补充：transient 为主、PIN 预案

**用户原话**："对,继续吧"（消息 226，回应 Agent 的"批准吗？"）。

**批准的路线**：第一版只做 transient 配对（固定 PIN 3939，无感）；如果真机证明 transient 不行，再做完整 PIN 配对预案。

> Agent 细节：预案的具体触发条件表述；诊断日志的具体内容清单（配对每步结果、错误内容、响应全文、发送计数）。

## 二、实施默认项

Agent 列出七条实施层默认方案（消息 257），用户回复"行"（消息 258）。

**认可的路线**（七条的大意）：

1. 分里程碑推进（探针 → 可播放 → 图形界面），可播放版交给用户测试；
2. 先用 44.1kHz/16bit/立体声站稳，再考虑高规格；
3. 延迟取保守默认值，调低的风险用户自负；
4. 找设备：自动发现 + 手动输 IP 两条路；
5. 断流要能自动恢复；
6. 交付形态是单个 exe 加日志文件；用户测试后把日志文件交给 Agent 分析，不需要自己描述现象。

> Agent 细节：延迟窗口的具体数值（11025/88200）；恢复的机制（新连接重配、指数退避、静音续传）；各里程碑的具体内容。
> 注：原第 5 条"第一版不做清单"（元数据/多声道/MediaRemote/设备密码/PTP/PIN 配对）经用户指示删除——"第一版"从未定义，该条无边界。（PIN 预案的触发条件仍在决策 E 补充中有效；PTP 推迟在决策 A 的 Agent 细节中。）
