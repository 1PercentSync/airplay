# 需求文档：Windows AirPlay 音频转发软件

> **本文档为冻结需求文档。** 冻结含义：自用户确认之日起至项目结束，本文档不得被修改，也不会存在任何新版本。
> Agent 仅被允许向用户**提议**修改；未获得用户明确授权时，禁止执行任何修改（未授权 == 不修改）。

## 1. 项目目标

开发一款运行于 Windows 的软件：

1. 捕获本机正在播放的音频；
2. 通过 AirPlay 协议将音频推送至**单个 HomePod** 播放。

## 2. 范围

### 2.1 范围内

- 运行平台：Windows 桌面（开发与测试环境为 Windows 11 + WSL Debian）。
- 音频捕获：**仅捕获虚拟音频设备**。
- 传输协议：AirPlay 1 或 AirPlay 2（选择规则见 §3.2）。
- 接收端：单个 HomePod。

### 2.2 范围外

- 视频 / 屏幕镜像。
- 多接收端、多房间同步播放。
- 对 HomePod 以外接收端的兼容性承诺（若自然兼容更佳，但不作为需求）。
- macOS / Linux 平台支持。

## 3. 功能性需求

### 3.1 音频捕获

- 仅捕获虚拟音频设备（例如 Sunshine 方案中的 "Steam Streaming Speakers"）。
  - 理由：AirPlay 链路延迟高，本地音箱无法与之同步监听，因此本软件仅从虚拟音频设备捕获，而非捕获默认输出设备。
  - 哪些音频被路由到虚拟音频设备由操作系统或其他软件决定，不属于本软件的范围与职责。
- 须调研所选虚拟音频设备的音质上限（采样率、位深、声道数、驱动重采样行为）；若 "Steam Streaming Speakers" 音质不足，须评估并选用更优的替代虚拟音频设备方案。
- 参考 Sunshine 的音频捕获实现方式，但以其源码的实际行为为准（见 §6）。

### 3.2 传输协议

- 决策规则：**在可获得的信息（协议规范、现有实现、可验证性）足以支撑满足 §4 优先级的前提下，优先选择更高的 AirPlay 协议版本**；信息不足则回退至信息充分的较低版本。
- 协议版本的最终选择须以研究阶段的证据链为依据，并形成书面决策记录。

### 3.3 基本可用性

- 提供启动 / 停止推流、查看连接状态的基本手段。
- 交互形态（GUI / 托盘 / CLI）由设计阶段确定并记录于设计文档，不属于本冻结需求。

## 4. 质量属性优先级（严格按序）

1. **稳定性**：在达到"尚可音质"底线的前提下，稳定性越高越好。
   - 长时间连续播放不断流；
   - 网络抖动 / 丢包下可自动恢复或优雅降级；
   - 接收端异常（断连、重启）可被检测并恢复。
2. **音质**：在第 1 条满足的前提下，音质越高越好。
   - 捕获 → 编码 → 传输 → 解码全链路各环节的参数（采样率 / 位深 / 编码格式）均须有据可查，并达到当前技术条件下的理论最优。
3. **延迟**：在第 1、2 条满足的前提下，延迟越低越好。

## 5. 工程与研究约束

- 允许参考与复用现有实现，但：
  - **不得盲从其 README / 文档声称的能力**，必须深入代码确认实际行为；
  - "能跑"不足够：须在 §4 优先级下达到理论最优，代码架构与工程质量亦须保持高标准（KISS、复用优先、代码变动时同步检查文档）。
- 一切技术声称须有佐证（源码位置或官方文档）；未经验证的内容一律标记为"假设"，不得作为结论。
- 依赖及其用法的调研仅使用官方渠道（Context7、DeepWiki、官方文档 / 源码）；网络检索不得包含年份 / 日期。
  - **特殊豁免**：AirPlay 为 Apple 私有协议，不存在公开的官方协议文档。因此 AirPlay 协议相关的调研（协议规范、配对与加密机制、对现有实现的分析与逆向工程等）不受官方渠道限制，可使用社区与逆向工程来源；但所有结论仍须以代码级证据或可复现验证为准，未验证内容仍标记为假设。
- 代码内文字使用英文；面向用户的界面文字在实现前与用户确认。

## 6. 开放问题（不冻结，由研究 / 设计阶段回答）

- HomePod 具体型号与系统版本（可能影响协议支持，待验证）。
- 交互形态（GUI / 托盘 / CLI）。
- 技术栈（语言 / 库），由研究阶段的证据决定。